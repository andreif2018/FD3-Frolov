import { SpriteBgDirective } from './sprite-bg.directive';

describe('SpriteBgDirective', () => {
  it('should create an instance', () => {
    const directive = new SpriteBgDirective();
    expect(directive).toBeTruthy();
  });
});
